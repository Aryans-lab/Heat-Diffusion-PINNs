## Data

This project does not rely on external labeled datasets.

Training data consists of randomly sampled collocation points in space–time,
generated programmatically using scripts in `src/data_generation/`.

No raw data files are required.
